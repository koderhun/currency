name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Install, build 🔧
        run: |
          npm install
          npm run build
          npm run deploy
